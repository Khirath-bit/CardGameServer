<UserControl x:Class="CardGame.Views.ChatMessageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CardGame.Views"
             mc:Ignorable="d" MinWidth="30">
    <Grid>
        <Border Margin="5" Padding="5" BorderThickness="1" BorderBrush="BLue" Background="White" CornerRadius="10">
            <TextBlock Text="{Binding Message, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
        </Border>
        <TextBlock HorizontalAlignment="Right" VerticalAlignment="Bottom" Background="Transparent" x:Name="DateTime"></TextBlock>
    </Grid>
</UserControl>
